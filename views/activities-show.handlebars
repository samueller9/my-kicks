<div class="row mt-4 text-white">
  <div class="col-lg-6 offset-lg-3">
    <img class="img-fluid w-100 rounded mb-3" src="{{activities.imgUrl}}" alt="Card image cap">
    <h3>{{activities.activity}}</h3>
    <div class="lead">{{activities.desc}}</div>
    <p>
      <a href="/activities" type="submit" class="btn btn-primary">Back</a>
      <a href="#" type="submit" class="btn btn-danger">Look at Shoes</a>
    </p>
  </div>
</div>
